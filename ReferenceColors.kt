package com.elevencapital.app

import androidx.compose.ui.graphics.Color

/** Dominant flat colors measured from the supplied PNGs; no new palette. */
internal object ReferenceColors {
    val Background = Color(0xFF08090E)
    val Card = Color(0xFF1A1B1F)
    val Buy = Color(0xFFADF47E)
    val DisabledAmount = Color(0xFF4A673B)
}
